import styled from 'styled-components';
import { DatePicker } from 'antd';

const { RangePicker } = DatePicker;

export const RangerPickerCustom = styled(RangePicker)`
  height: 40px;
  border-radius: 8px;
`;
